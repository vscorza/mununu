# Save EXP-0012 patch:
git diff HEAD -- crates/mununu-core/src/mu_calculus/evaluator.rs > /tmp/exp-0012-changed-flag.patch
cp crates/mununu-core/src/mu_calculus/evaluator.rs /tmp/evaluator-with-exp0012.rs

# A side (clone+compare baseline; SoA + EXP-0004 in tree):
# (revert just the EXP-0012 portions — helpers + loop refactor — by Edit)
scripts/bench_compare.sh exp-0012-changed-flag -- -p mununu-core --features test_support --bench mu_calculus_only

# B side (in-place + changed flag):
cp /tmp/evaluator-with-exp0012.rs crates/mununu-core/src/mu_calculus/evaluator.rs
scripts/bench_compare.sh exp-0012-changed-flag --baseline-only -- -p mununu-core --features test_support --bench mu_calculus_only

# Diff:
scripts/bench_diff.sh exp-0012-changed-flag --robust
