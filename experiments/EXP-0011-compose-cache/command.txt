# EXP-0011: Context-level composition memoization (plan §B3)
#
# The candidate change is in crates/mununu-core/src/context/mod.rs:
#   - Adds compose_cache: RefCell<HashMap<u64, (String, String, Discriminant, Arc<Clts>)>>
#   - Adds compose_named_cached(left, right, options) -> Arc<Clts>
#   - Cache key: u64 hash of (left, right, mode_discriminant) via DefaultHasher
#   - Collision-safe: stores full key alongside Arc, rechecks on hit
#
# The bench is in crates/mununu-core/benches/composition_only.rs::bench_compose_cache
# Fixtures use RandomClts with .with_uncontrollable_prefix(3) so the two CLTSs
# can share alphabet without tripping the controllability check during compose.
#
# Run (single side, both arms in one bench since cache is internal state):
cargo bench -p mununu-core --features test_support \
  --bench composition_only -- composition_only/cache_compare

# Quick sanity check (standalone binary):
cargo run --release -p mununu-core --features test_support --bin cache_check

# Test gate (must stay green):
cargo test -p mununu-core --features test_support
