# A side (std HashMap baseline):
git checkout HEAD -- crates/mununu-core/src/composition/mod.rs crates/mununu-core/Cargo.toml
# (then surgically restore sitting-2's Cargo.toml features without rustc-hash)
scripts/bench_compare.sh exp-0010-fxhash -- -p mununu-core --features test_support --bench composition_only

# B side (FxHashMap candidate):
# (apply the FxHashMap changes manually)
scripts/bench_compare.sh exp-0010-fxhash --baseline-only -- -p mununu-core --features test_support --bench composition_only

# Diff (with per-iter Mann-Whitney bug fix landed in same sitting):
scripts/bench_diff.sh exp-0010-fxhash --robust
