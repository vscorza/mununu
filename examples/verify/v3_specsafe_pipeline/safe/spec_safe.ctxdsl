// spec_safe.ctxdsl — self-composed speculative-load side channel, safe.
// Generated by generate.py — do not hand-edit; re-run the generator.
// State = (copyA cache footprint, copyB cache footprint); S = pre-resolution.
context Spec {
    automata {
        automaton Spec {
            controllable {
                label observe;
                label resolveA_n;
                label resolveB_n;
                label stutter;
            }
            states {
                state AS_BS initial;
                state AS_Bn;
                state An_BS;
                state An_Bn;
                state Agree;
                state Leak;
            }
            transitions {
                transition AS_BS -> An_BS on label resolveA_n;
                transition AS_BS -> AS_Bn on label resolveB_n;
                transition An_BS -> An_Bn on label resolveB_n;
                transition AS_Bn -> An_Bn on label resolveA_n;
                transition An_Bn -> Agree on label observe;
                transition Agree -> Agree on label stutter;
                transition Leak -> Leak on label stutter;
            }
            predicates { predicate leak = state Leak; predicate agree = state Agree; }
        }
    }
}
