# v3_specsafe_pipeline / safe — V.3 of the §Phase 7 domain-validation track.
# Controlling doc: docs/design/industrial-value-and-validation-domains.md §6
# (speculative-execution security / hyperproperties). GENERATED by ../generate.py
# — do not hand-edit.
#
# Self-composition of a speculative-load side channel. Discriminating verdict:
#   noninterference = true (the no-secret-dependent-cache-trace contract holds)
# Non-interference (a 2-safety hyperproperty) reduces to safety on the self-
# composed product: never(Leak), where Leak = the two copies' cache footprints
# differ under equal public input + differing secret. Research-grade abstract
# model (claims-integrity), NOT a production Spectre checker.

[project]
name = "V3_SpecSafe_Safe"
description = "V.3 speculative non-interference, safe design. Self-composed speculative-load side channel; never(Leak) is the contract-conformance verdict."

[[sources]]
id = "Spec"
adapter = "ctxdsl"
files = ["spec_safe.ctxdsl"]

[alphabet]
strategy = "direct"

[composition]
semantics = "asynchronous"
members = ["Spec"]
name = "SpecSystem"

[[properties]]
# Non-interference / contract conformance: the two copies' observable cache
# footprints never disagree ⇒ no secret-dependent cache trace.
name = "noninterference"
template = "never"
args = { BAD = "Leak" }
over = "SpecSystem"

[[properties]]
# Non-vacuity witness (dual of noninterference): the leak IS observable.
name = "leak_reachable"
template = "reachable"
args = { TARGET = "Leak" }
over = "SpecSystem"

[[properties]]
# Sanity: every reachable product state has an enabled transition.
name = "deadlock_freedom"
template = "no_deadlock"
over = "SpecSystem"
