# v2_tso_storebuffer / sc — V.2 of the §Phase 7 domain-validation track.
# Controlling doc: docs/design/industrial-value-and-validation-domains.md §5
# (memory consistency). GENERATED by ../generate.py — do not hand-edit.
#
# Store-buffering litmus (Thread0: x=1; r0=y  ||  Thread1: y=1; r1=x).
# Memory model: SC. Discriminating verdict:
#   both_read_zero_reachable = false (SC / the fence forbids r0=r1=0)
# This is the canonical SB result (herd / rmem / any MCM textbook).

[project]
name = "V2_SB_SC"
description = "V.2 store-buffering litmus, SC memory model. r0=r1=0 reachable iff the model relaxes write→read order."

[[sources]]
id = "SbSC"
adapter = "ctxdsl"
files = ["sb_sc.ctxdsl"]

[alphabet]
strategy = "direct"

[composition]
semantics = "asynchronous"
members = ["SbSC"]
name = "SbSystem"

[[properties]]
# The litmus check: is the both-read-zero outcome observable?
name = "both_read_zero_reachable"
template = "reachable"
args = { TARGET = "BothReadZero" }
over = "SbSystem"

[[properties]]
# Sanity: some non-both-zero outcome is always observable (both models).
name = "other_outcome_reachable"
template = "reachable"
args = { TARGET = "OtherOutcome" }
over = "SbSystem"
