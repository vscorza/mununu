{
  "$schema": "mununu_sv_multi_v1",
  "modules": [
    {
      "name": "producer",
      "source": "multi_producer.sv",
      "clock_domain": "clk_sys",
      "signals": [
        {"name": "state", "abstraction": "enum", "variants": ["IDLE", "PRODUCING", "DONE"]}
      ],
      "inputs": [
        {"name": "enable", "abstraction": "boolean"}
      ],
      "controllable": []
    },
    {
      "name": "consumer",
      "source": "multi_consumer.sv",
      "clock_domain": "clk_sys",
      "signals": [
        {"name": "state", "abstraction": "enum", "variants": ["IDLE", "BUSY", "ACK"]}
      ],
      "inputs": [],
      "controllable": []
    }
  ],
  "connections": [
    {
      "from": "producer.valid",
      "to": "consumer.valid",
      "abstraction": "boolean",
      "note": "Producer valid signal drives consumer"
    }
  ],
  "composition": {
    "mode": "synchronous",
    "name": "system"
  },
  "properties": [
    {
      "id": "no_deadlock",
      "formula": "nu X. ([] X)",
      "description": "Composed system has no deadlock"
    }
  ]
}
