# Notebook — 2026-W19, Fri PM (2026-05-06, sitting 6)

## Sitting 6 — Substack post 1 + EXP-0002b-mem (heap axis)

### Landed

- **Substack article post 1** drafted at [`publications/blog/01-five-experiments-and-a-protocol.md`](../publications/blog/01-five-experiments-and-a-protocol.md). ~2200 words, methodology-first framing covering EXP-0001's cold-cache contamination, the L3 protocol, EXP-0002b's confirmed 2.4× win, EXP-0010's falsification of plan §B2, and the bench_diff.sh per-iter bug. Status: `ready_for_review`. Ready to ship to Substack as the series opener.
- **Series outline updated**: post 1 now absorbs the original posts 1 and 4 into a single methodology-first piece. Post 2 anchors on EXP-0002b instead of the original EXP-0002. Outline rationale documented in [`publications/blog/00-series-outline.md`](../publications/blog/00-series-outline.md).
- **`crates/mununu-core/src/bin/dhat_synthesis.rs`**: dhat-instrumented binary that replays the EXP-0002b grid_32x32 synthesis workload. Uses `dhat::Alloc` as `#[global_allocator]` and `dhat::Profiler::new_heap()` around 3 synthesis iterations. Cargo.toml `[[bin]]` entry gates on `test_support + dhat` features.
- **EXP-0002b-mem** archived. Headline: SoA's 2.4× wall-clock win is **NOT** a heap-pressure win. Total bytes -0.02%, total allocations -0.008%, peak heap -76 KB (-6.7%), peak blocks identical. The pre-registered ≥100 KB heap-reduction hypothesis from EXP-0002 README is falsified at the grid_32x32 scale. Wall-clock confirmation from EXP-0002b stands.
- **ADR-0010** records the heap-axis falsification + the corrected SoA narrative for the paper.

### Headline result

| Metric | A (HashMap) | B (SoA) | Δ |
|--------|------------:|--------:|--:|
| Total bytes (3 synth runs) | 1,633,826,260 | 1,633,442,349 | -384 KB (-0.02%) |
| Total allocations | 28,856,332 | 28,853,942 | -2,390 (-0.008%) |
| Peak heap (t-gmax) | 1,141,316 | 1,064,732 | **-76,584 (-6.7%)** |
| Peak live blocks | 8,279 | 8,279 | 0 |

### Why this matters more than the raw numbers suggest

The SoA migration was sold (in EXP-0002 README) on TWO axes: wall-clock AND heap. EXP-0002b confirmed the wall-clock axis at 2.4× on grid_32x32. EXP-0002b-mem now refutes the heap axis at the same scale. **The win is real but the mechanism is different from what we pre-registered.**

For the paper §3.x:
- ✅ "SoA delivers 2.4× wall-clock speedup on synthesis-bound workloads with non-trivial alternation" (EXP-0002b)
- ✅ "The mechanism is cache locality + per-lookup cost, not heap pressure" (EXP-0002b-mem)
- ❌ "SoA reduces peak heap by ≥100 KB at synthesis-relevant scales" — falsified at grid_32x32 (76 KB observed)
- ⏳ "SoA's heap reduction scales with state count × num_fixpoint_vars" — testable in EXP-0002b-mem-deep at L4

This is a more precise, more defensible, more interesting story than the original. Honest falsifications strengthen the rest of the claims.

### Surprises / dead-ends

- *2026-05-06:* I expected the heap delta to be substantial because the iteration_ranks struct is the obvious memory difference. Empirically, iteration_ranks accounts for only ~76 KB at this scale; the rest of the 1+ MB heap is fixture data, formula AST, modal pre-image groups, etc. — none of which the SoA migration touches. Lesson: dhat is essential for this kind of mechanism attribution; intuition was wrong.
- *2026-05-06:* `cargo run --bin dhat_synthesis` failed at link time without `--features test_support,dhat`. The `[[bin]]` entry needed `required-features = ["test_support", "dhat"]` to gate it explicitly, otherwise `cargo build --bins` tries to build it with default features and breaks on missing `bench_support` and `dhat::Alloc`.
- *2026-05-06:* The dhat-mode binary runs ~5-10× slower than release mode (interception cost on every allocation). Wall-clock numbers from this binary are NOT comparable to Criterion bench numbers; only the heap profile is meaningful.

### Next sitting plan

1. **EXP-0003 (LabelSetTable interning)**: substantive change in the CLTS layer (vs §B2's drop-in). Replaces per-state HashMap<SmallVec<LabelId>, ...> with a global LabelSetId(u32) intern table. Larger lift than EXP-0002 (~1 day per the plan); higher expected payoff. Plan §A2.
2. **EXP-0001-deep + EXP-0002b-deep + EXP-0002b-mem-deep at L4** in `mununu-dev` container with Turbo Boost disabled. Required for paper-grade citation.
3. **`cargo-llvm-cov`** in Makefile + CI artifact (E7 in the plan).
4. **Reflect on plan §B-series falsification implications.** §B2 falsified; §B3 (composition cache) and §B4 (changed-flag termination) might be similarly drop-in-flavored. Re-rate them with ADR-0009/0010 in mind before scheduling.
