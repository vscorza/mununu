# Notebook — 2026-W19, Wed (2026-05-06)

## Foundation landed (EXP-0001-prep)

Scaffolding for the optimization programme is in place. No code change to the verifier yet — only the reproducibility infrastructure described in [`0000-overview.md`](0000-overview.md) and the plan file at `~/.claude/plans/do-a-deep-evaluation-sparkling-origami.md`.

Concretely:

- [`experiments/_template/`](../experiments/_template/) — eight-file experiment archive template (README, log.md, notes.md; manifest/command/hw-fingerprint/criterion-archive/dhat-archive added by `bench_record.sh` at run time).
- [`scripts/`](../scripts/) — `capture_hw.sh`, `new_experiment.sh`, `bench_record.sh`, `bench_diff.sh`, `repro.sh`, `check_repro.sh`, `plot_speedup.py`. Smoke-tested `capture_hw.sh` on the host (Intel i7-9750H, macOS 26.3 Darwin 25.3.0); fingerprint captures CPU/memory/cache/governor/toolchain as designed. `new_experiment.sh` round-trips through `check_repro.sh` cleanly.
- [`Makefile`](../Makefile) — added verbs: `test-fast`, `test-properties`, `proptest-deep`, `stress`, `fuzz`, `coverage`, `mem-profile`, `bench-baseline`, `bench-compare`, `bench-record`, `experiment`, `replay`, `publish-prep`. Existing verbs (`build`, `test`, `lint`, `verify`, `ci`, `clean`) preserved.
- [`notebook/decisions.md`](decisions.md) — ADR-0001 (reproducibility scaffold) and ADR-0002 (differential oracle precedes algorithmic swaps) and ADR-0003 (witness-rank audit gates chaotic iteration) recorded.
- [`publications/blog/00-series-outline.md`](../publications/blog/00-series-outline.md) and [`publications/paper/outline.md`](../publications/paper/outline.md) — the publication map; LaTeX moves to `mununu-private/paper/` per CLAUDE.md.
- `.gitignore` — added `**/bench-stdout.log` and `.repro-EXP-*/` so live logs don't pollute commits.

`make ci` exits 0 against the new Makefile (cargo fmt + cargo clippy --all-targets -D warnings + cargo test --workspace, full doctest pass).

## Next

EXP-0001 (baseline freeze) requires the subsystem benches that don't exist yet: `clts_construction.rs`, `composition_only.rs`, `minimization_only.rs`, `mu_calculus_only.rs`. Today's existing benches (`clts_composition.rs`, `mu_calculus.rs`, `controller.rs`, `xstate.rs`) measure end-to-end pipelines. The plan calls for a transitional period where the existing benches stay registered and the new isolated benches add coverage.

Order proposed for the next sitting (per the plan's landing sequence):

1. Add `rand`, `rand_chacha` to dev-deps; add `stress`, `test_support` features to `mununu-core/Cargo.toml`.
2. Write `crates/mununu-core/src/test_support.rs` with `RandomClts` + chain/ring/grid/hypercube/`worst_case_partition` templates (E3 first iteration).
3. Write `benches/_common.rs` (provenance recorder, fixture loader with checksum guard).
4. Write `benches/clts_construction.rs`, `composition_only.rs`, `minimization_only.rs`, `mu_calculus_only.rs` against the deterministic fixtures.
5. Run `make bench-record EXP=EXP-0001-baseline -- --bench '*_only'` to freeze EXP-0001.
6. Open EXP-0002-iter-rank-soa as the first optimization (lowest-risk, self-contained).

## Sitting 2 (2026-05-06) — iteration policy + first EXPs

Landed:

- **Iteration policy formalized.** `experiments/SCHEMA.md` describes the manifest schema versioning, `notebook/REFINEMENT.md` describes the three classes of change (additive optional, additive required, breaking → supersede). `notebook/decisions.md` carries ADR-0004 for the policy.
- **`.draft` marker convention.** `scripts/new_experiment.sh` drops a `.draft` file when scaffolding; `scripts/check_repro.sh` exempts drafts from manifest/command/hw-fingerprint requirements (still requires README.md + log.md). `scripts/bench_record.sh` removes the marker on a successful run, promoting the EXP to validated. SCHEMA.md documents the convention.
- **`--fresh` flag on `bench_record.sh`.** Clears `target/criterion/` before running so the archived JSON contains only what was just measured. EXP-0001 ran *without* `--fresh` (the archive serendipitously captured all four subsystems' smoke runs); this is documented as a quirk in `experiments/EXP-0001-baseline-cliff/notes.md` and EXP-0001-deep is on the followups list.
- **Cargo features added** to `crates/mununu-core/Cargo.toml`: `test_support` (pulls `rand` + `rand_chacha`), `stress` (depends on test_support), `dhat`. dev-deps gained `rand`, `rand_chacha`.
- **`crates/mununu-core/src/test_support.rs`** — deterministic CLTS generators using `rand_chacha::ChaCha20Rng`. Templates: `chain`, `ring`, `grid`, plus `RandomClts` builder with seed/states/density/alphabet/max_labels_per_edge knobs. 5 unit tests, all green.
- **`crates/mununu-core/src/bench_support.rs`** — fixture cache + `announce()` helper. Initially put under `benches/_common.rs` but cargo auto-detects every `benches/*.rs` as its own bench target; moved into the library, gated by `test_support` feature, accessed from benches via `use mununu_core::bench_support as common;`.
- **Four isolated benches** at `benches/{clts_construction,composition_only,minimization_only,mu_calculus_only}.rs`. Each registered in `Cargo.toml` with `required-features = ["test_support"]`. All four compile and produce sensible smoke numbers via `cargo bench --features test_support --bench <name> -- --quick`.
- **EXP-0001-baseline-cliff** archived: README + log + notes filled, full provenance manifest at schema_version 1, criterion-archive.tar.zst (185 KB) capturing the bench output, hw-fingerprint via `scripts/capture_hw.sh`. Headline: chain_1k minimization at **1.51 s** is the dramatic Paige-Tarjan target.
- **EXP-0002-iter-rank-soa** scaffolded (draft): README pre-registers the hypothesis (≥2× synthesis-bound speedup, ≥100 KB heap reduction, ±1% on non-synthesis benches), log/notes shaped for next-sitting completion. `.draft` marker present; `check_repro.sh` validates as draft.
- `make ci` exit 0; `scripts/check_repro.sh` exit 0 with EXP-0001 validated and EXP-0002 [DRAFT] flagged.

## Surprises / dead-ends

- macOS bash `set -e` interacts with `tar --use-compress-program=zstd` if zstd isn't installed; `bench_record.sh` will need a graceful fallback to gzip, plus a check at startup. Punting: will add when running EXP-0001 and discovering it on a clean machine. **Status (sitting 2): zstd was available on the host; archive wrote successfully.**
- `Makefile` `%:` catch-all for `bench-record EXP=... -- <args>` is a known make-ism. It works but emits a "warning: overriding recipe" that I'm ignoring; if it gets noisy in CI, switch to a wrapper script that consumes the rest of `$*`. **Status (sitting 2): bench_record.sh is being invoked directly from the shell, not via `make bench-record`, so the warning hasn't surfaced. If CI hits it, switch then.**
- `cargo`'s bench auto-detection treats every `benches/*.rs` as a target. The leading underscore in `_common.rs` did not exempt it. Resolution: shared bench helpers live at `crates/mununu-core/src/bench_support.rs` (gated by `test_support` feature), not in `benches/`.
- `StateId<u32>` does not implement `TryFrom<usize>`; only `from_index(usize) -> Option<Self>`. Caught during `cargo test --features test_support` of the new templates. The error message is clear; not a problem, just a friction worth noting for newcomers.
- `compose()` takes `&CompositionOptions`, not owned. Fixed in benches.
- `minimize_bisimulation` is at `composition::minimize::minimize_bisimulation`, not re-exported from `composition`. Used the qualified path. If a future refactor flattens this, multiple benches need to track the change in lock-step.
- EXP-0001's `criterion-archive.tar.zst` ended up containing data for all four subsystems even though the manifest's `command` field names only `clts_construction`. Cause: `target/criterion/` accumulates across runs. Fix: `--fresh` flag on `bench_record.sh`, used by all EXPs starting at EXP-0002. Documented in `experiments/EXP-0001-baseline-cliff/notes.md`.

## Next sitting plan

1. **Implement EXP-0002** (iter-rank SoA): replace `WitnessMap.iteration_ranks: HashMap<(usize, FormulaVarId), usize>` with `Vec<Vec<u32>>`. Keep HashMap path under a `#[cfg(test, feature = "iter_rank_oracle")]` gate for differential testing. Add a `synthesis_signature_read` bench. Run with `--fresh`, dhat-instrumented.
2. **Land soundness regression suite skeleton** at `crates/mununu-core/tests/soundness/` (per ADR-0002).
3. **Land first three property tests** under `tests/properties/`: CLTS round-trip, minimization idempotence, sync-composition commutativity.
4. **EXP-0001-deep** re-record: full Criterion samples, inside `mununu-dev` container, Turbo Boost off, `--fresh`. Supersedes the EXP-0001 host baseline.
