% 07_determinism.tex — Tutorial 07: Determinism vs Non-Determinism
\input{common/preamble}

\title{07 --- Determinism}
\subtitle{Deterministic vs Non-Deterministic Models}

\begin{document}

\maketitle

% ------------------------------------------------------------------
\begin{frame}{Deterministic Protocol}
  In a \textbf{deterministic} automaton, each (state, label) pair has \alert{exactly one} target:

  \bigskip
  \begin{center}
  \begin{tikzpicture}[
    ->,>=Stealth,
    node distance=2.8cm,
    every state/.style={fill=ubaBlue!10, draw=ubaBlue, thick, minimum size=1cm},
    every edge/.style={draw, thick},
  ]
    \node[state, initial]  (I)  {DetIdle};
    \node[state]           (P)  [right=of I]  {DetPending};
    \node[state]           (D)  [right=of P]  {DetDone};

    \path (I) edge              node[above] {\footnotesize det\_req}   (P)
          (P) edge              node[above] {\footnotesize det\_ack}   (D)
          (D) edge [bend left=30] node[below] {\footnotesize det\_reset} (I);
  \end{tikzpicture}
  \end{center}

  \bigskip
  From \textbf{DetPending}, \texttt{det\_ack} leads to \emph{exactly one} state: \textbf{DetDone}.

  No ambiguity --- the next state is fully determined by the current state and the label.
\end{frame}

% ------------------------------------------------------------------
\begin{frame}{Non-Deterministic Protocol}
  Now \texttt{nd\_ack} from \textbf{NdPending} leads to \alert{two different states}:

  \bigskip
  \begin{center}
  \begin{tikzpicture}[
    ->,>=Stealth,
    node distance=2.5cm,
    every state/.style={fill=ubaBlue!10, draw=ubaBlue, thick, minimum size=1cm},
    every edge/.style={draw, thick},
  ]
    \node[state, initial]  (I)  {NdIdle};
    \node[state]           (P)  [right=of I]   {NdPending};
    \node[state]           (D)  [above right=1cm and 2.5cm of P] {NdDone};
    \node[state]           (R)  [below right=1cm and 2.5cm of P] {NdRetry};

    \path (I) edge                node[above]       {\footnotesize nd\_req}   (P)
          (P) edge                node[above left]  {\footnotesize \alert{nd\_ack}}  (D)
          (P) edge                node[below left]  {\footnotesize \alert{nd\_ack}}  (R)
          (R) edge [bend right=20] node[below]      {\footnotesize nd\_retry} (P)
          (D) edge [bend left=40]  node[above]      {\footnotesize nd\_reset} (I);

    % Mark nd_ack as uncontrollable
    \node[below=0.3cm of P, font=\scriptsize, red!70!black] {nd\_ack is \textbf{uncontrollable}};
  \end{tikzpicture}
  \end{center}

  \bigskip
  The same label \texttt{nd\_ack} from the same state \textbf{NdPending} --- two possible outcomes.

  Non-determinism models \textbf{uncertainty}: the environment decides which branch is taken.
\end{frame}

% ------------------------------------------------------------------
\begin{frame}{Impact on Verification: Box vs Diamond}
  Non-determinism changes the meaning of modalities:

  \bigskip
  \begin{description}
    \item[$\Boxx$ --- Box (universal):] ``\emph{All} successors satisfy $\varphi$''

    \medskip
    $[\text{labels} = \{ \texttt{nd\_ack} \}]\; \textit{NdDone}$ is \alert{FALSE} for NdPending

    because one \texttt{nd\_ack}-successor is NdRetry, not NdDone.

    \bigskip
    \item[$\Dia$ --- Diamond (existential):] ``\emph{Some} successor satisfies $\varphi$''

    \medskip
    $\langle \text{labels} = \{ \texttt{nd\_ack} \} \rangle\; \textit{NdDone}$ is \textbf{TRUE} for NdPending

    because one \texttt{nd\_ack}-successor \emph{is} NdDone.
  \end{description}

  \bigskip
  In a \textbf{deterministic} model, box and diamond over a label agree (at most one successor).
\end{frame}

% ------------------------------------------------------------------
\begin{frame}[fragile]{Non-Deterministic Transitions in CTXDSL}
  \lstinputlisting[style=ctxdsl, firstline=46, lastline=69]{../examples/07_determinism.ctxdsl}

  \medskip
  Lines 63--64: two transitions from \textbf{NdPending} on the \emph{same} label \texttt{nd\_ack}.

  Note: \texttt{nd\_ack} is \alert{uncontrollable} --- the environment triggers it.
\end{frame}

% ------------------------------------------------------------------
\begin{frame}[fragile]{Formulas: Box vs Diamond}
  \lstinputlisting[style=ctxdsl, firstline=93, lastline=107]{../examples/07_determinism.ctxdsl}

  \medskip
  \begin{itemize}
    \item \ctxdsl{nd_done_reachable} uses $\langle\;\rangle$ (diamond) --- only \textbf{1/4} states satisfy, because nd\_ack is nondeterministic and the outcome might be NdRetry.
    \item \ctxdsl{nd_either_reachable} uses $\langle\;\rangle$ but targets \textbf{NdDone $\lor$ NdRetry} --- \textbf{4/4} states satisfy, because \emph{both} outcomes are acceptable.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------------
\begin{frame}{Nondeterminism and the Skolem Paradigm}
  In Mununu's game-theoretic semantics, a player chooses a \textbf{label}, not a specific transition:

  \bigskip
  \begin{enumerate}
    \item The controller (or environment) selects a label to fire.
    \item If that label has \alert{multiple transitions} from the current state, the outcome is \textbf{adversarial} --- the worst case is assumed.
    \item This is the \textbf{Skolem paradigm}: nondeterministic branching is resolved by the opponent.
  \end{enumerate}

  \bigskip
  \textbf{Example from the protocol:}
  \begin{itemize}
    \item \ctxdsl{nd_done_reachable} = \textbf{1/4} states --- from NdPending, diamond sees two nd\_ack outcomes; since one leads to NdRetry (not NdDone), the adversary can force that branch.
    \item \ctxdsl{nd_either_reachable} = \textbf{4/4} states --- both nd\_ack outcomes (NdDone and NdRetry) satisfy the goal, so the adversary cannot prevent it.
  \end{itemize}

  \bigskip
  The label's \textbf{controllability} determines \emph{who fires it}; nondeterminism determines \emph{what happens after}.
\end{frame}

% ------------------------------------------------------------------
\begin{frame}[fragile]{The \texttt{< (ctrl = environment) >} Operator}
  The \texttt{< (ctrl = environment) >} diamond quantifies over \textbf{uncontrollable} transitions only:

  \bigskip
  \lstinputlisting[style=ctxdsl, firstline=123, lastline=128]{../examples/07_determinism.ctxdsl}

  \medskip
  \begin{description}
    \item[\ctxdsl{nd_env_prevents_done}:] $\gfp X.\; (\lnot\text{NdDone}) \;\land\; \langle\text{ctrl}=\text{environment}\rangle\; X$

    ``The environment can keep the system away from NdDone forever.''
  \end{description}

  \bigskip
  \begin{itemize}
    \item Satisfied by \textbf{3/4} states (NdIdle, NdPending, NdRetry) --- the environment triggers uncontrollable \texttt{nd\_ack} and nondeterminism may steer to NdRetry indefinitely.
    \item This is the \emph{dual} of \ctxdsl{nd_ctrl_force_done}: what the controller cannot force, the environment can prevent.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------------
\begin{frame}{Takeaway}
  \begin{center}
  \begin{tabularx}{0.88\textwidth}{lX}
    \toprule
    \textbf{Concept}  & \textbf{Meaning} \\
    \midrule
    Deterministic      & Each (state, label) has at most one target. Fully predictable. \\
    Non-deterministic  & Same (state, label) can have multiple targets. Models uncertainty. \\
    \midrule
    Box $[\;]\;\varphi$          & Universal --- \emph{all} successors satisfy $\varphi$. \\
    Diamond $\langle\;\rangle\;\varphi$ & Existential --- \emph{some} successor satisfies $\varphi$. \\
    \bottomrule
  \end{tabularx}
  \end{center}

  \bigskip
  \textbf{Key insight:} Non-determinism is not a bug --- it models environments whose behavior
  we cannot predict. Choose box or diamond depending on whether you need \emph{guarantees for all}
  or \emph{existence of some} outcome.

  \medskip
  {\scriptsize\color{ubaGray} Reference: Milner, R. (1989) \emph{Communication and Concurrency}. Prentice Hall.}
\end{frame}

\end{document}
